<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kuyu.mapper.pcms.PcmsSupplierUserMapper">

    <!--  -->
    <select id="findBySupplier" resultType="com.kuyu.vo.pcms.PcmsSupplierUserVo">
        SELECT
        p.img AS img,
        p.open_name AS open_name,
        p.create_time AS create_time
        FROM pcms_supplier_user t
        LEFT JOIN pcms_user p ON p.openid=t.openid
        <where>
           t.vendor_id = #{vendor_id}
        </where>
        ORDER BY p.create_time DESC
    </select>

</mapper>